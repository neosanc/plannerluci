{
  "name": "mis-calorias",
  "version": "2.0.0",
  "private": true,
  "description": "Contador de calorías — versión Android",
  "scripts": {
    "copiar": "rm -rf www && mkdir -p www && cp -r ../calorias/. www/ && rm -f www/LEEME.md",
    "sync": "npm run copiar && cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
