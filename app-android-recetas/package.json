{
  "name": "mis-recetas",
  "version": "1.0.0",
  "private": true,
  "description": "Recetas con productos chilenos — versión Android",
  "scripts": {
    "copiar": "rm -rf www && mkdir -p www && cp -r ../recetas/. www/ && rm -f www/LEEME.md",
    "sync": "npm run copiar && cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
